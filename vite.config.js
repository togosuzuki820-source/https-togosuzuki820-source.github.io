import { defineConfig } from 'vite'

export default defineConfig({
  // ★重要：あなたのGitHubリポジトリ名を / で囲んで書いてください
  // 例：リポジトリ名が swahili-app なら '/swahili-app/'
  base: '/togosuzuki820-source.github.io/', 
})
